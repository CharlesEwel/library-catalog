<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Books</title>
  </head>
  <body>
    <h1>Books @Model.GetName()</h1>
    @if(Model.GetBooks().Count==0)
    {
      <p>
        No books have been added yet for this author
      </p>
    }
    else
    {
      <ul>
        @foreach(var book in Model.GetBooks())
        {
          <li><a href="/books/@book.GetId()">@book.GetTitle()</a></li>
        }
      </ul>
    }
    <a href="/authors">Return to Authors</a>
    <a href="/books/add">Add New Book</a>
    <a href="/">Home</a>
  </body>
</html>
